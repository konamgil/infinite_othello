{
  "name": "engine-neo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "core": "workspace:*",
    "shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}